# -*- python -*-
import glob
import subprocess
Import('env')

for f in env.Glob("*.py"):
    print "Running test: ", f
    app = str(f.abspath)
    if subprocess.call(app):
        print f, "FAILED"
        Exit(2)
