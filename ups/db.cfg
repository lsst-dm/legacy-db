# -*- python -*-

import lsst.sconsUtils

dependencies = {
    "required": ["mysqlclient", ],
}

config = lsst.sconsUtils.Configuration(
    __file__,
    headers=[], libs=[],
    hasSwigFiles=False,
)
